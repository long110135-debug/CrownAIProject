# CrownAI 架构审计报告
**日期:** 2026-07-28
**范围:** ~/CrownAIProject 全项目只读审计
**状态:** 84测试全通过，无严重漏洞，5个中等风险待修

---

## 项目实际调用链

```
LaunchAgent (7个)
    ↓
scheduler.py (纯命令分发 + 任务锁)
    ↓
pipeline/daily_run.py (唯一业务入口)
    ├── sync_today() → scraper/apifootball_data.py → DB:matches
    ├── track_odds() → scraper/apifootball_odds.py → crown_odds_collector.save_api_odds() → DB:odds_timeline
    ├── analyze_matches() → L2过滤 → 5模型 → crown_score → L3过滤 → DB:prediction_history
    ├── close_odds() → odds_tracker.lock_closing_odds() → DB:closing_odds
    ├── settle_matches() → settle.auto_settle() → DB:match_result/model_validation/model_contribution
    └── generate_reports() → daily_report + clv_analysis + odds_profile → output/*.html

crown_daemon.py (常驻进程，独立循环)
    ├── 浏览器登录/会话维持
    ├── _fetch_odds() → crown_scraper._parse_matches()
    └── crown_odds_collector.save_crown_odds() → DB:odds_timeline

main.py (手动CLI，纯参数解析 → 调用daily_run)
```

---

## 严重漏洞

**无。** 核心架构正确：单一业务入口、模型不访问DB、AI裁判不生成方向、odds写入单一通道、任务锁互斥。

---

## 中等风险

### M1. database.py 存在独立的 CLV 计算实现
- **文件:** `utils/database.py` 第794行 `calc_clv()`
- **问题:** 与 `utils/odds_math.calc_clv()` 是两套独立实现
- **后果:** 修改CLV公式时只改一处另一处不同步
- **修复:** `database.calc_clv()` 内部调用 `odds_math.calc_clv()`，自身只负责DB读写

### M2. database.py 存在独立的 `_handicap_to_number()`
- **文件:** `utils/database.py` 第1244行
- **问题:** 与 `utils/odds_math.handicap_to_number()` 算法相同但独立实现
- **后果:** 盘口格式扩展时两处解析不一致
- **修复:** 删除私有实现，改为 `from utils.odds_math import handicap_to_number`

### M3. crown_scraper.py 存在完整的重复判断逻辑
- **文件:** `scraper/crown_scraper.py` 第476行 `analyze_handicap_change()` + 第550行 `_handicap_to_number()`
- **问题:** 完整复制了 `odds_math.compute_change()` 的逻辑（早期遗留，未被主流程调用）
- **后果:** 误用时得到与主流程不一致的结果
- **修复:** 删除这两个函数

### M4. settle.py 内联盘口解析
- **文件:** `settle.py` 第215行 `_calc_handicap_result()`
- **问题:** 用 `re.findall(r'[\d.]+', asian)` 自己解析，未调用 `odds_math.handicap_to_number()`
- **后果:** 对 "客让"/"受"/负号前缀的处理可能与odds_math不一致
- **修复:** 替换为 `from utils.odds_math import handicap_to_number`

### M5. filter_funnel 表无唯一约束
- **文件:** `utils/database.py` `filter_funnel` 表定义
- **问题:** `log_date` 没有 UNIQUE 约束，同一天多次analyze产生重复行
- **后果:** 漏斗统计膨胀
- **修复:** 加 `log_date TEXT UNIQUE NOT NULL`

---

## 轻微问题

### L1. `_safe_float()` 定义了4次
- **位置:** odds_tracker / crown_scraper / hga_scraper / database
- **修复:** 移入 `utils/odds_math.py`

### L2. `_parse_time()` 定义了2次
- **位置:** daily_run.py / odds_tracker.py
- **修复:** 统一到 `utils/helpers.py`

### L3. crown_daemon.py 重复了 crown_scraper.py 的浏览器管理代码
- **位置:** daemon的 `_ensure_browser()/_wait_spa()` vs scraper的 `_start_browser()/_login()/_wait_spa_ready()`
- **修复:** 抽取为 `scraper/browser_session.py`

### L4. 四分之一盘(0.25/0.75)结算简化处理
- **文件:** `settle.py` `_calc_handicap_result()`
- **问题:** 0.25盘平局标记为 `away_cover`（全输），实际应为半输
- **后果:** 长期统计中0.25盘平局盈亏偏差50%
- **修复:** 增加 `half_win`/`half_loss` 返回值

### L5. Watchdog重启daemon可能与KeepAlive冲突
- **文件:** `scheduler.py` `_try_restart_daemon()`
- **问题:** plist的KeepAlive=true已自动重启，watchdog再kickstart可能双实例
- **修复:** 重启前pgrep确认不存在，或用 `launchctl kickstart -k`

---

## 重复代码汇总

| 重复内容 | 出现位置 | 应统一到 |
|---------|---------|---------|
| `handicap_to_number()` | odds_math / database / crown_scraper / settle(内联) | `utils/odds_math.py` |
| `compute_change()` 升盘降盘判断 | odds_math / crown_scraper | `utils/odds_math.py` |
| `calc_clv()` | odds_math / database | `utils/odds_math.py` |
| `_safe_float()` | 4个文件 | `utils/odds_math.py` |
| `_parse_time()` | daily_run / odds_tracker | `utils/helpers.py` |
| 浏览器启动/登录/SPA等待 | daemon / crown_scraper | `scraper/browser_session.py` |

---

## 缺失测试

| 缺失测试 | 风险 | 优先级 |
|---------|------|--------|
| test_clv_calculation.py — 端到端CLV计算验证 | CLV公式错误无法发现 | 高 |
| test_settle_quarter_ball.py — 0.25/0.75盘half_win/half_loss | 盈亏统计偏差 | 高 |
| test_concurrent_track_analyze.py — 并发读写一致性 | 数据不一致 | 中 |
| test_crown_collector_normalization.py — 联赛名/日期/match_id | 数据孤岛 | 中 |
| test_watchdog_restart_storm.py — 冷却机制验证 | 重启风暴 | 中 |
| test_filter_funnel_dedup.py — 同日去重 | 统计膨胀 | 低 |
| test_odds_timeline_ordering.py — 时间线排序 | 初盘识别错误 | 低 |
| test_model_weight_consistency.py — 权重热更新 | 权重不同步 | 低 |

---

## 建议修改顺序

1. **M5** filter_funnel加UNIQUE约束（1行SQL）
2. **M2** 删除database.py的`_handicap_to_number`，改用odds_math
3. **M1** database.calc_clv()委托odds_math.calc_clv()
4. **M4** settle.py改用odds_math.handicap_to_number
5. **M3** 删除crown_scraper.py的死代码
6. **L4** 实现half_win/half_loss精确结算
7. **L5** watchdog重启前确认无存活进程
8. 补充缺失测试
9. **L1-L3** 工具函数去重

预计工作量: 2-3小时，不涉及架构变更。

---

## 已验证的架构约束

- [x] main.py / scheduler.py 无业务逻辑（测试验证）
- [x] daily_run.py 是唯一业务入口（测试验证）
- [x] 5个模型不访问DB（源码检查 + 测试验证）
- [x] AI裁判不生成方向、不重复分析（测试验证）
- [x] odds写入通过crown_odds_collector单一通道（源码检查）
- [x] odds_tracker委托odds_math做升盘降盘判断（测试验证）
- [x] 权重集中在config/settings.py + 启动assert（测试验证）
- [x] track/analyze互斥锁（测试验证）
- [x] 时间线追加不覆盖（测试验证）
- [x] prediction_history唯一match_id（测试验证）
- [x] 推荐记录含完整快照（model_version/weights/ai_decision/odds_water）
